import { LoadingAnimationContainer, LoadingAnimationWrapper } from "./styled";

const LoadingAnimation = () => {
    return (
        <LoadingAnimationContainer>
            <LoadingAnimationWrapper>
                <p>Loading...</p>
            </LoadingAnimationWrapper>
        </LoadingAnimationContainer>
    );
};

export default LoadingAnimation;
