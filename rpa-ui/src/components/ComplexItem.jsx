const ComplexItem = ({ complex }) => {
    return (
        <div>
            <div>
                {complex.name} {complex.description}
            </div>
        </div>
    );
};

export default ComplexItem;
